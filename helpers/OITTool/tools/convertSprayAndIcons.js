const fs = require('fs')
const { exec } = require('child_process')

//mogrify -format png *.dds
//pngquant * --ext=.png --speed 1 --force --strip

const convertFiles = args => {

}

module.exports = { convertFiles }